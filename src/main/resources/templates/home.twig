<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Home - Quest Store</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="static/css/home.css">
    </head>
    <body>
        <wrapper>
            <header>
                <img id="cc-logo-header" src="static/img/codecool-logo.png">
                <h1>Quest Store</h1>
                <div class="logged-user">
                    <div class="user-info">
                        <p>{{userName}}</p>
                        <div class="horizontal">
                            "<p>{{userNickname}}</p>"
                        </div>
                    </div>
                    <img class="user-avatar" src="static/img/avatarExample.jpeg">
                </div>
            </header>
            
            <nav>
                <ul>
                    {% for name, href in options %}
                        <li><a href="{{href}}">{{name}}</a></li>
                    {% endfor %}
                </ul>
            </nav>
            <content>
                {% include "/templates/example.twig" ignore missing %}
            </content>
            <footer>
                <p>2018 &copy; Radian & Ferned</p>
            </footer>
        </wrapper>   
    </body>
</html>